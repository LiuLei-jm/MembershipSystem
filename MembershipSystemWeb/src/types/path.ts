export interface PathConfiguration {
  basePath: string
  membershipCardFilePath: string
  allowCustomPaths: boolean
}

export interface PathConfigurationUpdateRequest {
  basePath: string
  membershipCardFilePath: string
  allowCustomPaths: boolean
}
