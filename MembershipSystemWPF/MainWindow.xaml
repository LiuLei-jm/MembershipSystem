<Window
    x:Class="MembershipSystemWPF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MembershipSystemWPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="限时会员网关"
    Width="720"
    Height="580"
    MinWidth="720"
    MinHeight="580"
    Closing="Window_Closing"
    Loaded="Window_Loaded"
    ResizeMode="CanMinimize"
    StateChanged="Window_StateChanged"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label VerticalAlignment="Center" Content="服务器地址:" />
        <TextBox
            x:Name="ServerUrlTextBox"
            Grid.Column="1"
            Width="140"
            Margin="5" />
        <StackPanel
            Grid.Column="2"
            Margin="5,0,0,0"
            Orientation="Horizontal">
            <Label VerticalAlignment="Center" Content="设备名:" />
            <TextBox
                x:Name="DeviceName"
                Width="140"
                Margin="5" />
        </StackPanel>
        <StackPanel
            Grid.Row="0"
            Grid.Column="3"
            Margin="5,0,0,0"
            Orientation="Horizontal">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Width" Value="70" />
                    <Setter Property="Margin" Value="10,0,0,0" />
                    <Setter Property="Padding" Value="3" />
                </Style>
            </StackPanel.Resources>
            <Button
                x:Name="ConnectButton"
                Click="ConnectButton_Click"
                Content="连接" />
            <Button
                x:Name="SaveButton"
                Click="SaveButton_Click"
                Content="保存" />
            <Button
                x:Name="ClearButton"
                Click="ClearButton_Click"
                Content="清除日志" />
        </StackPanel>
        <Label
            Grid.Row="1"
            Grid.Column="0"
            VerticalAlignment="Center"
            Content="通讯密钥:" />
        <TextBox
            x:Name="ApiKeyTextBox"
            Grid.Row="1"
            Grid.Column="1"
            Grid.ColumnSpan="3"
            Height="120"
            Margin="5"
            TextWrapping="Wrap" />
        <GroupBox
            Grid.Row="3"
            Grid.Column="0"
            Grid.ColumnSpan="4"
            Margin="0,10,0,0"
            Header="日志">
            <TextBox
                x:Name="LogTextBox"
                IsReadOnly="true"
                TextWrapping="Wrap"
                VerticalScrollBarVisibility="Auto" />
        </GroupBox>
    </Grid>
</Window>
